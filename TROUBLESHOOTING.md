# 🔧 Решение проблем

## Проблема: TLS handshake timeout

Если вы видите ошибку:
```
panic: Post "https://api.telegram.org/bot.../getMe": net/http: TLS handshake timeout
```

Это означает, что ваше приложение не может подключиться к Telegram API.

### Возможные причины:

1. **Telegram API заблокирован в вашем регионе** (самая частая причина)
2. Проблемы с интернет-соединением
3. Файрвол блокирует подключение
4. Проблемы с DNS

---

## ✅ Решения

### Решение 1: Использование прокси (рекомендуется)

Если Telegram заблокирован в вашем регионе, используйте прокси или VPN.

#### Вариант A: Через переменные окружения в .env

Добавьте в ваш `.env` файл:

```bash
TELEGRAM_BOT_TOKEN=your_token_here
HTTPS_PROXY=http://proxy.example.com:8080
```

Или для SOCKS5 прокси:
```bash
HTTPS_PROXY=socks5://proxy.example.com:1080
```

#### Вариант B: Через системные переменные окружения

```bash
export HTTPS_PROXY=http://proxy.example.com:8080
go run main.go
```

#### Вариант C: Использование VPN

Если у вас настроен VPN на системе, бот автоматически будет использовать его.

---

### Решение 2: Проверка доступности API

Проверьте, доступен ли Telegram API:

```bash
curl -I --connect-timeout 5 https://api.telegram.org
```

Если команда зависает или выдает таймаут - API недоступен, нужен прокси/VPN.

---

### Решение 3: Использование альтернативного API endpoint

Если у вас есть собственный прокси-сервер для Telegram API, можно использовать его:

```go
// В коде можно изменить endpoint
bot, err := tgbotapi.NewBotAPIWithClient(
    token, 
    "https://your-proxy-server.com/bot",  // кастомный endpoint
    httpClient,
)
```

---

## 🔍 Диагностика

### Шаг 1: Проверьте интернет-соединение

```bash
ping 8.8.8.8
```

### Шаг 2: Проверьте доступность Telegram API

```bash
curl -v https://api.telegram.org
```

### Шаг 3: Проверьте DNS

```bash
nslookup api.telegram.org
```

### Шаг 4: Проверьте, что токен правильный

Токен должен начинаться с цифр и содержать двоеточие:
```
123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

## 💡 Рекомендации

### Для разработки:
- Используйте `.env` файл с прокси (если нужен)
- Не коммитьте `.env` в git (уже в `.gitignore`)

### Для production:
- Используйте системные переменные окружения
- Настройте прокси на уровне сервера/контейнера
- Используйте VPN или прокси-сервер

---

## 🆘 Если ничего не помогает

1. **Проверьте логи** - в коде теперь более подробные сообщения об ошибках
2. **Попробуйте другой прокси** - возможно, текущий не работает
3. **Проверьте файрвол** - возможно, он блокирует подключения
4. **Обратитесь к провайдеру** - возможно, есть проблемы на их стороне

---

## 📝 Примеры настройки прокси

### HTTP прокси:
```bash
HTTPS_PROXY=http://username:password@proxy.example.com:8080
```

### SOCKS5 прокси:
```bash
HTTPS_PROXY=socks5://username:password@proxy.example.com:1080
```

### Без аутентификации:
```bash
HTTPS_PROXY=http://proxy.example.com:8080
```

---

**После настройки прокси перезапустите бота!**

