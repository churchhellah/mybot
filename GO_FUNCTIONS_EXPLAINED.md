# 📚 Функции в Go: Правила определения

## ❌ НЕПРАВИЛЬНО: Функция внутри другой функции

В Go **нельзя** определять функции внутри других функций (в отличие от JavaScript, Python и др.):

```go
func main() {
    // ❌ ЭТО НЕ РАБОТАЕТ В GO!
    func formatWelcomeMessage(username string) string {
        return "Hello, " + username
    }
    
    msg := formatWelcomeMessage("John")
}
```

**Ошибка компиляции:** `syntax error: unexpected func, expecting expression`

---

## ✅ ПРАВИЛЬНО: Функция на уровне пакета

Функции в Go должны быть определены **на уровне пакета** (вне других функций):

```go
package main

import "fmt"

// ✅ ПРАВИЛЬНО: функция на уровне пакета
func formatWelcomeMessage(username string) string {
    if username == "" {
        return "Привет! Добро пожаловать!"
    }
    return "Привет, " + username + "! Добро пожаловать!"
}

func main() {
    // Теперь можно использовать функцию
    msg := formatWelcomeMessage("John")
    fmt.Println(msg)
}
```

---

## 📍 Где располагать функции?

### Вариант 1: Перед main() (рекомендуется)

```go
package main

import "fmt"

// Вспомогательные функции обычно размещают перед main
func helperFunction() {
    // ...
}

func anotherFunction() {
    // ...
}

func main() {
    helperFunction()
    anotherFunction()
}
```

**Плюсы:**
- Легко найти все функции
- Логичный порядок: вспомогательные → главная функция

### Вариант 2: После main()

```go
package main

import "fmt"

func main() {
    helperFunction()
}

// Функции можно определять и после main
func helperFunction() {
    fmt.Println("Hello")
}
```

**Плюсы:**
- main() видна сразу при открытии файла
- Подходит для небольших файлов

---

## 🎯 Рекомендации по структуре файла

### Для небольших проектов (как наш бот):

```go
package main

import (
    // импорты
)

// 1. Вспомогательные функции
func formatWelcomeMessage(username string) string {
    // ...
}

func sendMessageSafely(bot *Bot, chatID int64, text string) error {
    // ...
}

// 2. Главная функция
func main() {
    // основной код
}
```

### Для больших проектов:

Разделяйте код на несколько файлов и пакетов:

```
project/
├── main.go          # только main()
├── handlers.go      # обработчики команд
├── utils.go         # вспомогательные функции
└── models.go        # структуры данных
```

---

## 💡 Важные моменты

### 1. Порядок определения не важен

```go
func main() {
    callFunction() // можно вызвать функцию, определенную ниже
}

func callFunction() {
    fmt.Println("Called!")
}
```

Go компилирует весь файл сразу, поэтому порядок не важен.

### 2. Видимость функций

- Функции с **заглавной буквы** - экспортируемые (публичные):
  ```go
  func PublicFunction() {} // можно использовать в других пакетах
  ```

- Функции с **строчной буквы** - приватные (только в текущем пакете):
  ```go
  func privateFunction() {} // только в этом пакете
  ```

### 3. Анонимные функции (замыкания)

В Go можно создавать анонимные функции, но они используются как значения:

```go
func main() {
    // ✅ Это работает - анонимная функция как значение
    greet := func(name string) {
        fmt.Println("Hello,", name)
    }
    
    greet("John")
    
    // Или сразу вызвать
    func(msg string) {
        fmt.Println(msg)
    }("Hello!")
}
```

---

## 🎓 Практическое задание

Попробуйте создать несколько функций в вашем боте:

1. `formatWelcomeMessage` - уже создана ✅
2. `sendMessageSafely` - безопасная отправка сообщений
3. `createUserFromMessage` - создание пользователя из сообщения

Все функции должны быть на уровне пакета, вне `main()`!

---

## 📝 Итог

- ✅ Функции определяются **на уровне пакета** (вне других функций)
- ✅ Порядок определения не важен
- ✅ Можно размещать до или после `main()` - на ваш выбор
- ❌ Нельзя определять функции внутри других функций

**Помните:** Go - компилируемый язык, все функции должны быть известны на этапе компиляции!

