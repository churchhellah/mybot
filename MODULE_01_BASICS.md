# 📘 Модуль 1: Основы Go через Telegram бота

## 🎯 Цель модуля
Изучить базовые концепции Go через улучшение вашего Telegram бота.

---

## 📚 Урок 1: Переменные, типы и функции

### Теория (5 минут)

#### Переменные в Go
```go
// Явное объявление типа
var name string = "Go"

// Короткое объявление (тип выводится автоматически)
name := "Go"

// Несколько переменных
var x, y int = 1, 2
x, y := 1, 2
```

#### Основные типы
- `string` - строки
- `int`, `int8`, `int16`, `int32`, `int64` - целые числа
- `uint`, `uint8`, `uint16`, `uint32`, `uint64` - беззнаковые целые
- `float32`, `float64` - числа с плавающей точкой
- `bool` - логический тип (true/false)
- `byte` - алиас для uint8
- `rune` - алиас для int32 (символ Unicode)

#### Функции
```go
// Обычная функция
func add(a int, b int) int {
    return a + b
}

// Множественные возвращаемые значения (характерно для Go!)
func divide(a, b float64) (float64, error) {
    if b == 0 {
        return 0, errors.New("division by zero")
    }
    return a / b, nil
}

// Именованные возвращаемые значения
func calculate(x, y int) (sum int, product int) {
    sum = x + y
    product = x * y
    return // автоматически вернет sum и product
}
```

### 🏋️ Практическое задание 1.1

**Задача:** Добавьте функцию для форматирования приветственного сообщения.

**Требования:**
1. Создайте функцию `formatWelcomeMessage(username string) string`
2. Функция должна возвращать персонализированное приветствие
3. Используйте эту функцию в команде `/start`

**Подсказка:**
```go
func formatWelcomeMessage(username string) string {
    // Ваш код здесь
    return fmt.Sprintf("Привет, %s! Добро пожаловать!", username)
}
```

---

## 📚 Урок 2: Структуры (Structs)

### Теория (5 минут)

Структуры в Go - это коллекции полей. Они похожи на классы в других языках, но без наследования.

```go
// Определение структуры
type User struct {
    ID       int64
    Username string
    FirstName string
    LastName  string
}

// Создание экземпляра
user := User{
    ID:        123,
    Username:  "john_doe",
    FirstName: "John",
    LastName:  "Doe",
}

// Доступ к полям
fmt.Println(user.Username) // john_doe

// Указатели на структуры
userPtr := &user
userPtr.Username = "new_name" // изменяет оригинал
```

### 🏋️ Практическое задание 1.2

**Задача:** Создайте структуру для хранения информации о пользователе бота.

**Требования:**
1. Создайте структуру `BotUser` с полями:
   - `ID` (int64)
   - `Username` (string)
   - `FirstName` (string)
   - `JoinedAt` (time.Time)
2. Создайте функцию `createUserFromMessage(msg *tgbotapi.Message) BotUser`
3. Сохраняйте информацию о пользователе при команде `/start`

---

## 📚 Урок 3: Указатели

### Теория (5 минут)

Go использует указатели для передачи ссылок на значения.

```go
// Создание указателя
x := 42
p := &x  // p теперь указатель на x

// Разыменование указателя
fmt.Println(*p) // 42

// Изменение через указатель
*p = 21
fmt.Println(x) // 21

// Указатели в функциях
func increment(n *int) {
    *n++ // изменяет оригинальное значение
}

num := 5
increment(&num)
fmt.Println(num) // 6
```

**Когда использовать указатели:**
- Когда нужно изменить значение внутри функции
- Для больших структур (избегаем копирования)
- Для передачи nil (отсутствие значения)

### 🏋️ Практическое задание 1.3

**Задача:** Добавьте счетчик сообщений для каждого пользователя.

**Требования:**
1. Добавьте поле `MessageCount` в структуру `BotUser`
2. Создайте функцию `incrementMessageCount(user *BotUser)` используя указатель
3. Обновляйте счетчик при каждом сообщении

---

## 📚 Урок 4: Интерфейсы

### Теория (5 минут)

Интерфейсы в Go определяют набор методов. Тип реализует интерфейс, если реализует все его методы.

```go
// Определение интерфейса
type Messenger interface {
    Send(message string) error
    Receive() (string, error)
}

// Структура реализует интерфейс, если имеет все методы
type TelegramBot struct {
    token string
}

func (b *TelegramBot) Send(message string) error {
    // реализация
    return nil
}

func (b *TelegramBot) Receive() (string, error) {
    // реализация
    return "", nil
}
```

**Особенность Go:** Интерфейсы реализуются неявно. Не нужно явно указывать `implements`.

### 🏋️ Практическое задание 1.4

**Задача:** Создайте интерфейс для обработчиков команд.

**Требования:**
1. Создайте интерфейс `CommandHandler` с методом `Handle(update *tgbotapi.Update, bot *tgbotapi.BotAPI)`
2. Создайте структуры `StartCommand` и `HelpCommand`, реализующие этот интерфейс
3. Рефакторите код, используя эти обработчики

---

## 📚 Урок 5: Обработка ошибок

### Теория (5 минут)

В Go нет исключений. Ошибки возвращаются как обычные значения.

```go
// Ошибка - это интерфейс
type error interface {
    Error() string
}

// Создание ошибки
err := errors.New("something went wrong")
err := fmt.Errorf("error: %v", someValue)

// Проверка ошибки
result, err := someFunction()
if err != nil {
    log.Printf("Error: %v", err)
    return err
}

// Игнорирование ошибки (не рекомендуется!)
result, _ := someFunction()
```

**Лучшие практики:**
- Всегда проверяйте ошибки
- Не игнорируйте ошибки без причины
- Возвращайте понятные сообщения об ошибках

### 🏋️ Практическое задание 1.5

**Задача:** Улучшите обработку ошибок в боте.

**Требования:**
1. Обработайте ошибку при отправке сообщения (`bot.Send`)
2. Логируйте ошибки вместо паники
3. Создайте функцию `sendMessageSafely` с правильной обработкой ошибок

---

## 🎯 Итоговое задание модуля 1

**Задача:** Рефакторинг бота с использованием всех изученных концепций.

**Требования:**
1. ✅ Используйте структуры для данных пользователя
2. ✅ Создайте интерфейс для обработчиков команд
3. ✅ Правильная обработка ошибок
4. ✅ Разделите код на логические функции
5. ✅ Используйте указатели где необходимо

**Критерии оценки:**
- Код читаемый и структурированный
- Нет паник (кроме критических ошибок запуска)
- Все ошибки обработаны
- Использованы изученные концепции

---

## 📝 Чеклист прогресса

- [ ] Урок 1: Переменные и функции
- [ ] Урок 2: Структуры
- [ ] Урок 3: Указатели
- [ ] Урок 4: Интерфейсы
- [ ] Урок 5: Обработка ошибок
- [ ] Итоговое задание выполнено

---

## 🚀 Следующий шаг

После завершения модуля 1 переходим к **Модулю 2: Пакеты и модули**, где научимся организовывать код в пакеты и работать с зависимостями.

**Удачи! Помните: практика - ключ к успеху!** 💪

